spring:
  profiles:
    active:
      - local
    group:
      local: "site-local,db-local"
      dev: "site-dev,db-local"
    include:
      - db
      - my-service
      - site
  main:
    banner-mode: off
  banner:
    image:
      bitdepth: 8
      width: 100
      height: 15
