type PetType = IDog | ICat;

// interface IPet extends PetType {} // An interface may only extend a class or another interface.

// class Pet implements PetType {} // A class may only implement another class or interface.

interface IDog {}
interface ICat {}
