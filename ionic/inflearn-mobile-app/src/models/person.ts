export class Person {
  avatar: string = '';
  name: string = '';
  detail: string = '';

  constructor(avatar, name, detail) {
    this.avatar = avatar;
    this.name = name;
    this.detail = detail;
  }
}
