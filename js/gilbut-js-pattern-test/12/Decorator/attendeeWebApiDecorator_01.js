// attendeeWebApiDecorator 초기 코드

Conference.attendeeWebApiDecorator = function(baseWebapi) {
  'use strict';

  return {
    post: function post(attendee) {

    },

    getAll: function getAll() {
      
    }
  }
}
